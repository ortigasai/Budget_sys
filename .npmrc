; The project path contains "&" (OneDrive folder name). npm's default Windows
; script-shell (cmd.exe) mis-resolves .cmd shims when the cwd contains "&",
; breaking every "npm run" script. Use Git Bash instead, which handles it fine.
script-shell=C:\\Users\\jumalonpvp\\AppData\\Local\\Programs\\Git\\usr\\bin\\bash.exe
